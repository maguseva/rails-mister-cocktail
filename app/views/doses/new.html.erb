<div class="banner">
  <div class="container mt-5">
    <h1>New Ingredients</h1>

    <%= render 'form', cocktail: @cocktail, dose: @dose %>

    <%= link_to 'Back', cocktail_path(@cocktail), class: "btn btn-secondary mt-4"%>

  </div>
</div>
